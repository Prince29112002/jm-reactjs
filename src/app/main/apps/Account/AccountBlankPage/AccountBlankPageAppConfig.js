
import AccountBlankPage from './AccountBlankPage';

export const AccountBlankPageAppConfig = {
    settings: {
        layout: {
            config: {
                
            }
        }
    },
    routes  : [
        {
            path     : '/dashboard/accounts',
            component: AccountBlankPage
        }
    ]
};
